<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hellobody.model.dao.RespectDao">

	<insert id="addRespect" parameterType="Record">
		INSERT INTO (uId,cId)
		VALUES (uId=#{uId},cId=#{cId})
	</insert>

</mapper>
